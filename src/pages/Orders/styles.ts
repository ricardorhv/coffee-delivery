import styled from 'styled-components'

export const OrdersContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2rem;
  padding: 2rem 0;
`

export const OrderTable = styled.main`
  display: flex;
  flex-direction: column;
  gap: 0.25rem;

  min-width: 850px;
`
